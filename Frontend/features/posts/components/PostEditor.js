import React from 'react';

export const PostEditor = ({ initialContent = '' }) => {
  return (
    <div className="post-editor">
      {/* Post editor component */}
    </div>
  );
};

export default PostEditor;
